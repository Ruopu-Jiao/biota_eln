# `@biota/db`

Prisma schema placeholder for the ELN database layer.

This package will eventually own:

- Auth and session tables
- Organizations, repositories, folders
- Entries, entry versions, and blocks
- Biological entities, sequences, and annotations
- Protocols, relations, tables, and attachments
